// Display name for the paid tier.
//
// The entitlement *identifier* is also `Ante+` and lives in
// services/purchasesService.js — it must match the RevenueCat dashboard
// exactly and must never be changed for cosmetic reasons. This constant is
// purely what the user reads. One name, used everywhere, so the product
// never appears under two spellings.
export const PLUS_NAME = 'Ante+';
